using System;
using libStreamSDK;
//using SpotifyAPI.Web;
//using SpotifyAPI.Web.Models;

namespace focusify
{
    class Program
    {
        [STAThread]
        static void Main(string[] args)
        {

            /*SpotifyWebAPI api = new SpotifyWebAPI
            {
                AccessToken = "XX?X?X",
                TokenType = "Bearer"
            };

            FullTrack track = api.GetTrack("1eV81a6H4xDdpi8r2C4tQT");
            if (!track.HasError())
            {
                Console.WriteLine(track.Name);
            }
            else
            {
                Console.WriteLine("ERROR");
            }*/
            //ThinkgearController thinkgearController = new ThinkgearController();
            //int connectionID = thinkgearController.connect();
            //thinkgearController.readPackets(connectionID, 10);
            //thinkgearController.disconnect(connectionID);

            /* End program */
            //Console.ReadLine();

        }
    }
}
